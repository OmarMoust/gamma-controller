<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:GammaController.Converters">

    <!-- Colors -->
    <Color x:Key="BackgroundColor">#CC202020</Color>
    <Color x:Key="SurfaceColor">#40FFFFFF</Color>
    <Color x:Key="TextPrimaryColor">#FFFFFFFF</Color>
    <Color x:Key="TextSecondaryColor">#99FFFFFF</Color>
    <Color x:Key="BorderColor">#33FFFFFF</Color>
    <Color x:Key="SliderTrackColor">#40FFFFFF</Color>
    <Color x:Key="ThumbOuterColor">#FF4A4A4A</Color>
    
    <!-- Brushes -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="SliderTrackBrush" Color="{StaticResource SliderTrackColor}"/>
    <SolidColorBrush x:Key="ThumbOuterBrush" Color="{StaticResource ThumbOuterColor}"/>

    <!-- Slider Thumb (Windows 11 style) -->
    <Style x:Key="SliderThumbStyle" TargetType="Thumb">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid>
                        <!-- Dark gray outer circle -->
                        <Ellipse x:Name="ThumbCircle"
                                 Width="20" Height="20"
                                 Fill="{StaticResource ThumbOuterBrush}"
                                 StrokeThickness="0">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.3" Color="Black"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <!-- Inner accent circle - default size (small) -->
                        <Ellipse x:Name="AccentDot"
                                 Width="10" Height="10"
                                 Fill="{DynamicResource AccentBrush}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Hover: inner circle grows larger but with padding -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsDragging" Value="False"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard x:Name="HoverEnterStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="AccentDot"
                                                         Storyboard.TargetProperty="Width"
                                                         To="14" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="AccentDot"
                                                         Storyboard.TargetProperty="Height"
                                                         To="14" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="HoverEnterStoryboard"/>
                                <BeginStoryboard x:Name="HoverExitStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="AccentDot"
                                                         Storyboard.TargetProperty="Width"
                                                         To="10" Duration="0:0:0.1"
                                                         FillBehavior="Stop"/>
                                        <DoubleAnimation Storyboard.TargetName="AccentDot"
                                                         Storyboard.TargetProperty="Height"
                                                         To="10" Duration="0:0:0.1"
                                                         FillBehavior="Stop"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                        <!-- Pressed/Dragging: inner circle shrinks slightly -->
                        <Trigger Property="IsDragging" Value="True">
                            <Trigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="HoverEnterStoryboard"/>
                                <BeginStoryboard x:Name="DragEnterStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="AccentDot"
                                                         Storyboard.TargetProperty="Width"
                                                         To="8" Duration="0:0:0.05"/>
                                        <DoubleAnimation Storyboard.TargetName="AccentDot"
                                                         Storyboard.TargetProperty="Height"
                                                         To="8" Duration="0:0:0.05"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="DragEnterStoryboard"/>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="AccentDot"
                                                         Storyboard.TargetProperty="Width"
                                                         To="10" Duration="0:0:0.1"
                                                         FillBehavior="Stop"/>
                                        <DoubleAnimation Storyboard.TargetName="AccentDot"
                                                         Storyboard.TargetProperty="Height"
                                                         To="10" Duration="0:0:0.1"
                                                         FillBehavior="Stop"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Horizontal Slider (Windows 11 style) -->
    <Style x:Key="FluentSlider" TargetType="Slider">
        <Setter Property="Height" Value="32"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Track background -->
                        <Border x:Name="TrackBackground"
                                Height="4"
                                CornerRadius="2"
                                Background="{StaticResource SliderTrackBrush}"/>
                        
                        <!-- Filled portion (accent color) -->
                        <Border x:Name="TrackFill"
                                Height="4"
                                CornerRadius="2"
                                HorizontalAlignment="Left"
                                Background="{DynamicResource AccentBrush}">
                            <Border.Width>
                                <MultiBinding Converter="{x:Static converters:SliderFillConverter.Instance}">
                                    <Binding Path="Value" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="Minimum" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="Maximum" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource TemplatedParent}"/>
                                </MultiBinding>
                            </Border.Width>
                        </Border>
                        
                        <!-- Thumb track -->
                        <Track x:Name="PART_Track">
                            <Track.Thumb>
                                <Thumb Style="{StaticResource SliderThumbStyle}"/>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Style (Windows 11) -->
    <Style x:Key="FluentComboBox" TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid x:Name="MainGrid">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <ContentPresenter Grid.Column="0"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  IsHitTestVisible="False"/>
                                
                                <Path Grid.Column="1"
                                      Data="M0,0 L4,4 L8,0"
                                      Stroke="{StaticResource TextSecondaryBrush}"
                                      StrokeThickness="1.5"
                                      Margin="0,0,12,0"
                                      VerticalAlignment="Center"
                                      IsHitTestVisible="False"/>
                                
                                <!-- Invisible toggle button that covers the whole area -->
                                <ToggleButton x:Name="ToggleButton"
                                              Grid.ColumnSpan="2"
                                              Focusable="False"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press"
                                              Template="{x:Null}">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border Background="Transparent"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                        </Border>
                        
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               PopupAnimation="Slide"
                               Width="{Binding ActualWidth, ElementName=MainGrid}">
                            <Border Background="#FF2D2D2D"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Margin="0,2,0,0"
                                    Padding="4"
                                    MinWidth="{Binding ActualWidth, ElementName=MainGrid}">
                                <ScrollViewer MaxHeight="200">
                                    <StackPanel IsItemsHost="True"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#30000000"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBoxItem Style -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Border"
                            Background="Transparent"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#30FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
